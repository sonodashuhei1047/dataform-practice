config {
  type: "incremental"
}

SELECT
  sale_date,
  SUM(amount) AS total_amount
FROM `dataform-practice-494015.practice.sales`
${when(incremental(), `
WHERE sale_date > (
  SELECT MAX(sale_date) FROM ${self()}
)
`)}
GROUP BY sale_date