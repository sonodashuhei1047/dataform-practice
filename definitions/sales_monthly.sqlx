config {
  type: "table",
  dependencies: ["assert_sales_daily"]
}

SELECT
  DATE_TRUNC(sale_date, MONTH) AS month,
  SUM(total_amount) AS monthly_amount
FROM ${ref("sales_daily")}
GROUP BY month